// Central place for public frontend configuration.
// Note: VITE_* values are embedded in the browser bundle and are not secrets.
export const apiBaseUrl = import.meta.env.VITE_API_BASE_URL || 'http://localhost:8080';
