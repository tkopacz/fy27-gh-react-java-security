# Client objections

- "If Java is secure, the frontend is safe too." — Not true. The browser has its own attack surface.
- "We already scan Java, so we are covered." — Not for XSS, open redirect, and prototype pollution in React.
- "Dependency Review is only for the backend." — The frontend npm ecosystem needs the same guardrails.
