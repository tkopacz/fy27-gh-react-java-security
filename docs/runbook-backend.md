# Runbook backend

1. Uruchom backend zgodnie z `backend/README.md`.
2. Sprawdź `GET /api/products`.
3. Wyślij poprawne i błędne `POST /api/orders`.
4. Pokaż, że błędy walidacji są zwracane w formacie `fieldErrors`.
5. Przejdź do `frontend/README.md` i połącz frontend z backendem.
