# Presenter runbook

- Start the backend with `mvn spring-boot:run`.
- Start the frontend with `npm run dev`.
- Open the main routes and real APIs.
- Use the security-demo pages as controlled examples, not as production guidance.
- Explain that GHAS will show different alert classes for JavaScript/TypeScript and Java.
