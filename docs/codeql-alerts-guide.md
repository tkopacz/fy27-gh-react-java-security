# CodeQL alerts guide

## JavaScript / TypeScript

- `js/xss` — vulnerable DOM rendering with `dangerouslySetInnerHTML`
- `js/client-side-unvalidated-url-redirection` — unsafe redirect flow

## Java

- `java/sql-injection` — SQL string concatenation in the security demo
- `java/path-injection` — unsafe file path handling in the security demo
